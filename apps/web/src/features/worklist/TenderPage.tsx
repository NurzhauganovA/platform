/**
 * Тендерный отбор. Тонкая обёртка: экран общий для всех трёх разделов.
 *
 * Заведения закупки папкой здесь нет намеренно. Разбор идёт у тендерщика на
 * машине, где эти папки и лежат, — платформа показывает то, что он уже
 * разобрал, вместе с решениями модели и находками на рынках. Кнопка «загрузить
 * папку», которая при этом ничего не ускоряет, только предлагала бы сделать
 * работу второй раз.
 */

import { WorklistPage } from "./WorklistPage";

export function TenderPage() {
  return (
    <WorklistPage
      slug="tender"
      title="Отбор закупок"
      subtitle="Разобранные тендеры: за что браться и сколько на этом заработаем"
      unit="закупок"
      emptyHint="Разобранных закупок пока нет. Разбор папок идёт на машине тендерщика — как только он его прогонит, закупки появятся здесь сами."
    />
  );
}
