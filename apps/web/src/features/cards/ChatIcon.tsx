/**
 * Значок переписки: два пузыря разговора.
 *
 * Единственное место, где цвет задан не ролью палитры, а прямо. Это знак, а не
 * состояние: по нему находят кнопку среди прочих, и он должен быть узнаваем
 * сам по себе. Правило «цвет из ролей» стоит ради различимости при
 * дальтонизме — здесь оно и не нарушено: рядом со значком всегда слово
 * «Переписка», и цвет ничего не сообщает в одиночку.
 *
 * Градиент привязан к самой рамке (`objectBoundingBox`), а не к точкам холста:
 * значок рисуется и в 20 точек в кнопке, и крупнее в шапке окна, и во втором
 * случае жёстко заданные координаты увели бы переход за края.
 */

export function ChatIcon({ size = 20 }: { size?: number }) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      aria-hidden
      focusable="false"
    >
      <defs>
        <linearGradient id="fintend-chat" x1="0" y1="0" x2="1" y2="1">
          <stop offset="0" stopColor="#C820E0" />
          <stop offset="0.55" stopColor="#7A18E8" />
          <stop offset="1" stopColor="#2018E8" />
        </linearGradient>
      </defs>
      <g
        stroke="url(#fintend-chat)"
        strokeWidth="1.5"
        strokeLinecap="round"
        strokeLinejoin="round"
      >
        {/* Передний пузырь с хвостом влево вниз */}
        <path d="M9.4 2.4h3.9c3.8 0 6.9 2.6 6.9 5.9s-3.1 5.9-6.9 5.9H9.4c-.5 0-1 0-1.5-.1l-3 2.3c-.5.4-1.2 0-1.1-.6l.4-2.6C2.1 12.1 1 10.2 1 8.3c0-3.3 3.1-5.9 6.9-5.9h1.5Z" />
        {/* Строки разговора внутри */}
        <path d="M5.4 6.1h1M8.4 6.1h6.6M5.4 8.4h6.6M14 8.4h1M5.4 10.7h1M8.4 10.7h6.6" />
        {/* Задний пузырь: видна только правая дуга и хвост вправо вниз */}
        <path d="M20.4 11.7c1.5 1.1 2.4 2.7 2.4 4.4 0 1.8-1 3.4-2.6 4.5l.3 2c.1.6-.6 1-1.1.6l-2.3-1.7c-.6.1-1.2.2-1.8.2-1.7 0-3.2-.5-4.4-1.3" />
      </g>
    </svg>
  );
}
