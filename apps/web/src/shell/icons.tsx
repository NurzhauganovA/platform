/**
 * Значки пунктов меню.
 *
 * Имя значка объявляет сам модуль (`NavItem.icon`) — оболочка про разделы
 * по-прежнему не знает, она только рисует то, что назвали. Неизвестное имя
 * даёт точку: пункт без значка выпадал бы из ряда и выглядел сломанным, а
 * добавить значок под новое имя — правка на пять строк здесь, а не в модуле.
 *
 * Все нарисованы одной рукой: обводка в полторы точки, скруглённые концы,
 * сетка 24. Набор из готовой библиотеки тянет за собой сотню килобайт ради
 * девяти картинок, и половина из них нарисована в другой манере.
 *
 * Цвет берётся от текста (`currentColor`): значок в меню повторяет состояние
 * пункта, а не спорит с ним своим оттенком.
 */

const PATHS: Record<string, string> = {
  // Очередь работы: строки со значками слева.
  list: "M4 7h.01M4 12h.01M4 17h.01M9 7h11M9 12h11M9 17h7",
  // Таблица лотов.
  table: "M3.5 5.5h17v13h-17zM3.5 10h17M9.5 10v8.5",
  // Календарь подачи.
  calendar: "M4 6.5h16v14H4zM4 10.5h16M8.5 3.5v4M15.5 3.5v4",
  // Согласование: подпись поставлена.
  check: "M4.5 5.5h15v15h-15zM8 12.5l3 3 5.5-6",
  // Аналитика: столбики.
  chart: "M4 20V10M10 20V4M16 20v-7M21.5 20H2.5",
  // Обсуждения: пузырь разговора.
  chat: "M20.5 12c0 4-3.8 7.2-8.5 7.2-1 0-2-.15-2.9-.4l-4.6 2.3 1-4.3C3.4 15.6 2.5 13.9 2.5 12c0-4 3.8-7.2 8.5-7.2h1c4.7 0 8.5 3.2 8.5 7.2z",
  // Отбор: цель.
  target:
    "M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18zM12 16.5a4.5 4.5 0 1 0 0-9 4.5 4.5 0 0 0 0 9zM12 13.2a1.2 1.2 0 1 0 0-2.4 1.2 1.2 0 0 0 0 2.4z",
  // Закупки площадки: портфель.
  briefcase:
    "M3.5 8.5h17v11h-17zM9 8.5V6a1.5 1.5 0 0 1 1.5-1.5h3A1.5 1.5 0 0 1 15 6v2.5M3.5 13h17",
  // Уведомления: колокол. Без язычка внутри — на восемнадцати точках он
  // сливается с обводкой в пятно, и колокол перестаёт читаться колоколом.
  bell: "M18 16.5H6l1.5-2.5V10a4.5 4.5 0 0 1 9 0v4zM10.5 19.5a1.5 1.5 0 0 0 3 0",
  // Настройка обхода: шестерня, упрощённая до звезды с кругом.
  settings:
    "M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6zM12 2.5v3M12 18.5v3M21.5 12h-3M5.5 12h-3M18.7 5.3l-2.1 2.1M7.4 16.6l-2.1 2.1M18.7 18.7l-2.1-2.1M7.4 7.4L5.3 5.3",
};

export function NavIcon({ name }: { name?: string | null }) {
  const path = name ? PATHS[name] : undefined;

  return (
    <svg
      width="18"
      height="18"
      viewBox="0 0 24 24"
      fill="none"
      aria-hidden
      focusable="false"
      className="shrink-0"
    >
      {path ? (
        <path
          d={path}
          stroke="currentColor"
          strokeWidth="1.6"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
      ) : (
        <circle cx="12" cy="12" r="3.2" fill="currentColor" />
      )}
    </svg>
  );
}
