"""Кто какие колонки видит в списке закупов.

Отдельным файлом, потому что это решение про доступ, а не про вёрстку. Таблица
собирается из колонок самого проекта, и колонка, добавленная там, попала бы в
ответ сама — вместе с себестоимостью. Здесь стоит явный список, а незнакомая
колонка считается денежной и достаётся только тендерщику.

Читать этот файл нужно так: «что увидит закупщик, если откроет адрес руками».
Прятать колонку в браузере и отдавать её в JSON — самый распространённый
способ отдать себестоимость наружу.

Граница проведена по работе, а не по секретности. Закупщику нужно знать, где
взять товар и почём его отдаёт поставщик, — это и есть его работа. Не нужно
знать, во что позиция обходится нам после пошлины и логистики и сколько мы на
ней зарабатываем: для закупа это лишнее, а уходит вместе с человеком.
"""

from __future__ import annotations

from platform_api.modules.table import Visibility

POLICY: dict[str, Visibility] = {
    # --- что покупают: видно всем ---
    # Цена площадки не наша тайна: её видит любой участник закупа.
    "№ закупки": Visibility.ALL,
    "Решение": Visibility.ALL,
    "Товар": Visibility.ALL,
    "Заказчик": Visibility.ALL,
    "Цена торга": Visibility.ALL,
    "Кол-во": Visibility.ALL,
    "Приём до": Visibility.ALL,
    "Торг": Visibility.ALL,
    # --- работа закупщика: где брать и что проверить ---
    # «Где купить» — цена поставщика, а не наша себестоимость: пошлина,
    # логистика и накладные в неё не входят. Договариваться о ней закупщику.
    "Где купить": Visibility.SOURCING,
    "Marten": Visibility.SOURCING,
    "Рынок SKStore": Visibility.SOURCING,
    "Разбор": Visibility.SOURCING,
    # --- деньги: только тендерщику ---
    # «Почему» и «ИИ» выглядят как пояснения, но внутри проценты маржи:
    # «маржа 24% от pulscen.ru». Отдать их закупщику — то же самое, что
    # отдать колонку «Маржа %», только незаметнее.
    "Почему": Visibility.MONEY,
    "ИИ": Visibility.MONEY,
    "Себестоимость": Visibility.MONEY,
    "Откуда цена": Visibility.MONEY,
    "Расчёт себестоимости": Visibility.MONEY,
    "Маржа %": Visibility.MONEY,
    "Маржа ₸": Visibility.MONEY,
}

CODE_PREFIX = "SK"
"""Приставка постоянных кодов закупов SKStore: «SK-00042».

Своя у каждого раздела: впереди Mitwork и госзакупки, и «сорок вторая» без
приставки будет в каждом из них."""

ROLES: dict[str, str] = {
    "Товар": "title",
    "Заказчик": "customer",
    "Цена торга": "price",
    "Кол-во": "quantity",
    "Себестоимость": "cost",
    "Маржа %": "margin",
    "Маржа ₸": "profit",
}
"""Что колонка означает по смыслу — для итогов по отобранному, вида фильтра
и поиска.

Не дублирование заголовка: заголовки у разделов свои («Маржа ₸» здесь,
«Заработок всего, ₸» у OMarket, «заработок» у тендеров), а считается по ним
одно и то же. Роль — это то общее, что браузер может понять, не разбирая
русский текст.

`category` и `customer` — по ним аналитика группирует. Без них она не
сломается: разрез выбирается из пригодных колонок, а роль лишь задаёт, какая
из них открывается по умолчанию.

Колонки без роли обычные: их показывают и по ним фильтруют по значению, но в
итогах они не участвуют.
"""

ESSENTIAL: tuple[str, ...] = (
    # Номер закупки на площадке. Не порядковый номер строки: этим номером
    # закуп ищут в кабинете и называют в переписке с заказчиком, и до сих пор
    # за ним приходилось разворачивать «Все колонки» или открывать разбор.
    "№ закупки",
    "Товар",
    "Приём до",
    "Кол-во",
    "Цена торга",
    "Себестоимость",
    "Маржа %",
    "Маржа ₸",
    "Где купить",
)
"""Что видно сразу, без прокрутки вбок.

Вердикта здесь нет намеренно: его показывает цветная метка в первой колонке,
и держать рядом ещё и слово на восемнадцать знаков — значит занимать место
дважды. Слово никуда не делось: оно в подписи к метке, в легенде над таблицей
и в разборе. По кнопке «Все колонки» возвращается и сама колонка — там состав
дословно совпадает с книгой.

Книга даёт девятнадцать колонок, и это правильно для файла: там прокручивают
и сравнивают. На экране девятнадцать колонок означают, что маржа оказывается
за краем — а ради неё человек сюда и пришёл.

Отбор ровно по одному признаку: нужно ли это, чтобы решить «беру или нет».
Всё остальное — заказчик, ссылки, комментарий модели, разбор — открывается
переключателем «Все колонки», и там состав уже дословно совпадает с книгой.
"""

COMPACT: frozenset[str] = frozenset({"Где купить"})
"""Колонки, которые в списке показываются значком.

«Где купить» — это площадка, поставщик, цена, срок и минимальная партия одной
строкой: сорок четыре знака, из-за которых строка списка разъезжается на три
линии, а маржа уезжает за край. В списке от неё нужен один ответ — нашли или
нет, — и ссылка. Всё остальное открывается разбором, за подробностями идут
туда.
"""

__all__ = ["CODE_PREFIX", "COMPACT", "ESSENTIAL", "POLICY"]
