"""Кто какие колонки видит в списке госзакупок.

Отдельным файлом, потому что это решение про доступ, а не про вёрстку. Таблица
собирается из колонок книги ядра, и колонка, добавленная там, попала бы в
ответ сама. Здесь стоит явный список, а незнакомая колонка считается денежной
и достаётся только тендерщику.

Читать так: «что увидит закупщик, если откроет адрес руками». Прятать колонку
в браузере и отдавать её в JSON — самый распространённый способ отдать лишнее.

С госзакупками случай проще, чем с площадками: своей себестоимости и маржи
здесь пока нет, а плановая сумма заказчика — не наша тайна. Её видит любой
участник закупки на портале, и закрывать её значило бы прятать то, что
открыто всем.
"""

from __future__ import annotations

from platform_api.modules.table import Visibility

POLICY: dict[str, Visibility] = {
    # --- что за закупка ---
    "Площадка": Visibility.ALL,
    "№Закупки": Visibility.ALL,
    "ссылка на обявления": Visibility.ALL,
    "Способ закупки": Visibility.ALL,
    "Статус": Visibility.ALL,
    # --- сроки: по ним и решают, успеваем ли ---
    "Срок начала приема заявок": Visibility.ALL,
    "Срок окончания приема заявок": Visibility.ALL,
    "Осталось": Visibility.ALL,
    "Подано заявок": Visibility.ALL,
    # --- кто и что ---
    "Организация": Visibility.ALL,
    "БИН заказчика": Visibility.ALL,
    "наименование товара": Visibility.ALL,
    # --- деньги заказчика: открыты на портале всем участникам ---
    "цена": Visibility.ALL,
    "кол-во": Visibility.ALL,
    "Ед. изм.": Visibility.ALL,
    "сумма": Visibility.ALL,
    "Аванс, %": Visibility.ALL,
    # --- что именно покупают: работа закупщика ---
    "Код ЕНС ТРУ": Visibility.ALL,
    "Наименование ТРУ": Visibility.ALL,
    "Краткая характеристика": Visibility.SOURCING,
    "Дополнительная характеристика": Visibility.SOURCING,
    "Место поставки (КАТО)": Visibility.SOURCING,
    "Срок поставки": Visibility.SOURCING,
    "Условия ИНКОТЕРМС": Visibility.SOURCING,
    # --- про объявление ---
    "Наименование объявления": Visibility.ALL,
    "Статус объявления": Visibility.ALL,
    "Дата публикации": Visibility.ALL,
}

ESSENTIAL: frozenset[str] = frozenset(
    {
        "Площадка",
        "№Закупки",
        "наименование товара",
        "Организация",
        "сумма",
        "Осталось",
        "Код ЕНС ТРУ",
    }
)
"""Колонки, которые видны без прокрутки вбок.

Их шесть, и это ответ на вопрос «брать ли»: что покупают, у кого, за сколько
и сколько осталось времени. Остальное открывается кнопкой «все колонки» — в
книге их двадцать шесть, и на экран они не помещаются.
"""

ROLES: dict[str, str] = {
    "№Закупки": "code",
    "наименование товара": "title",
    "сумма": "amount",
    "Организация": "customer",
}
"""Роли колонок для браузера: где название, где сумма, где заказчик.

Разбирать заголовки на месте нельзя — у разделов они разные («Товар»,
«наименование товара»), и правило по ним сломается на следующей площадке.
"""

COMPACT: frozenset[str] = frozenset()
"""Колонок-значков здесь нет: всё, что показывается, читается текстом."""

CODE_PREFIX = "GZ"
CODE_WIDTH = 6
CODE_SEPARATOR = ""
"""Устойчивый код строки: «GZ000001».

Без разделителя и в шесть знаков — так его и просили назвать. У тендеров
вид другой («TN-00042»): та приставка выдана давно, ею закупки называют
вслух, и менять её форму значило бы разослать новые имена для того же."""
