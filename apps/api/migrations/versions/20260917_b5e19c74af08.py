"""Статуса «Новый» больше нет: взяли в работу — значит в работе.

«Новый» означал «появился в выгрузке, никто не смотрел». Но карточку заводит
тот, кто уже посмотрел: лот с портала берут руками или выборкой по номеру, и в
обоих случаях за ним стоит решение человека. Отдельный шаг между «взяли» и
«работаем» все пролистывали не глядя, а первым вопросом по нему было «а что с
ним делать».

Лоты, застрявшие в «Новом», переводятся в «В работе». Это не потеря: они и так
были взяты, просто никто не нажал следующую кнопку.
"""

from __future__ import annotations

from alembic import op

revision = "b5e19c74af08"
down_revision = "a2d740c96b13"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # `native_enum=False` хранит ИМЯ члена заглавными — отсюда 'NEW', а не 'new'.
    op.execute("update lot_cards set status = 'WORK' where status = 'NEW'")
    # В ленте событий переходы остаются как были: она отвечает на вопрос «что
    # тогда произошло», и переписывать её значит стирать историю. «Перевёл в
    # „Новый“» в старых записях — правда о том дне.


def downgrade() -> None:
    # Обратно не восстановить: какие из лотов «В работе» были новыми, после
    # перевода не отличить — признак и был самим статусом.
    pass
